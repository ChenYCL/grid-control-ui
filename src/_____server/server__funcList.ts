import { 支持的品种T } from "./支持的品种";

export const server__funcList = {
    set: {
        symbol: '' as 支持的品种T,
        参数: {
            a: 1,
            b: 1,
            count: 1,
            size: 0.01,
            dx: 0.05,
        },
    },
    start: {
        symbol: '' as 支持的品种T,
    },
    stop: {
        symbol: '' as 支持的品种T,
    },
}